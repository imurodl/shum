---
phase: 01-remote-host-registration-and-canonical-compose-discovery
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - internal/cli/project_inspect.go
  - internal/projects/inspect/context.go
  - internal/projects/inspect/service.go
  - internal/projects/inspect/output.go
  - internal/projects/inspect/storage.go
  - internal/projects/inspect/artifacts.go
  - internal/projects/inspect/service_test.go
  - test/integration/compose_inspect_test.go
  - test/e2e/remote_host_inspect_test.go
  - docs/testing.md
  - README.md
  - .gitignore
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/01-remote-host-registration-and-canonical-compose-discovery/01-VALIDATION.md
autonomous: true
requirements:
  - HOST-03
user_setup: []
must_haves:
  truths:
    - "`shum project inspect <ssh-alias> <project-ref>` shows host trust, project identity, canonical status, services, and storage surfaces in a summary-first view."
    - "Canonical inspect persists the exact Compose context tuple, env fingerprint, runtime snapshots, and rendered-config artifacts for later phases instead of rediscovering heuristically."
    - "Ambiguous or blocked inspect states stay visible with clear reasons when profiles, env, files, or render failures prevent canonicalization."
  artifacts:
    - path: "internal/cli/project_inspect.go"
      provides: "CLI entrypoint for canonical project inspection"
      min_lines: 80
      contains: "NewProjectInspectCommand"
    - path: "internal/projects/inspect/service.go"
      provides: "Canonical inspect orchestration over remote docker compose commands"
      min_lines: 120
      contains: "docker compose"
    - path: "internal/projects/inspect/storage.go"
      provides: "Storage surface extraction from compose, container, and volume data"
      min_lines: 80
      contains: "Mounts"
    - path: "internal/projects/inspect/output.go"
      provides: "Summary-first human output plus JSON and drill-down rendering"
      min_lines: 80
      contains: "--show-config"
    - path: "test/integration/compose_inspect_test.go"
      provides: "Compose integration coverage for canonical, ambiguous, blocked, and storage-surface inspect cases"
      min_lines: 80
      contains: "Test"
    - path: "README.md"
      provides: "Phase 1 operator quickstart and artifact-location documentation"
      min_lines: 40
      contains: "shum host register"
    - path: ".planning/phases/01-remote-host-registration-and-canonical-compose-discovery/01-VALIDATION.md"
      provides: "Updated validation map with real plan IDs and commands"
      contains: "01-01"
  key_links:
    - from: "internal/cli/project_inspect.go"
      to: "internal/projects/inspect/service.go"
      via: "Project inspect command delegates to canonical inspect service"
      pattern: "inspect\\.(NewService|Service)|Inspect\\("
    - from: "internal/projects/inspect/service.go"
      to: "internal/projects/discovery/service.go"
      via: "Inspect reuses discovery inventory and project metadata instead of rediscovering from scratch"
      pattern: "discovery|project"
    - from: "internal/projects/inspect/service.go"
      to: "internal/projects/inspect/artifacts.go"
      via: "Canonical inspect persists rendered config and runtime snapshots as artifacts"
      pattern: "artifact|snapshot|persist"
    - from: "internal/projects/inspect/service.go"
      to: "internal/projects/inspect/storage.go"
      via: "Inspect enriches rendered config with container and volume storage surfaces"
      pattern: "storage|Mounts|volume"
---

<objective>
Implement canonical Compose inspection, artifact persistence, and Phase 1 documentation and planning updates.

Purpose: The product cannot safely plan upgrades until it can explain the exact rendered Compose model, trust state, project identity, and storage surfaces for a target project.
Output: `project inspect` with canonical-context persistence and storage analysis, full HOST-03 test coverage, operator docs, and updated planning artifacts that reflect completed Phase 1 execution.
</objective>

<execution_context>
@/home/max/.codex/get-shit-done/workflows/execute-plan.md
@/home/max/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-remote-host-registration-and-canonical-compose-discovery/01-CONTEXT.md
@.planning/phases/01-remote-host-registration-and-canonical-compose-discovery/01-RESEARCH.md
@.planning/phases/01-remote-host-registration-and-canonical-compose-discovery/01-VALIDATION.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement canonical inspect orchestration and persisted context artifacts</name>
  <files>internal/cli/project_inspect.go, internal/projects/inspect/context.go, internal/projects/inspect/service.go, internal/projects/inspect/storage.go, internal/projects/inspect/artifacts.go</files>
  <action>Implement `project inspect &lt;ssh-alias&gt; &lt;project-ref&gt;` around remote `docker compose config --format json` plus the supporting `config --environment`, `config --profiles`, `config --services`, `config --volumes`, `config --networks`, `config --variables`, `docker compose ps --format json`, `docker inspect`, and `docker volume inspect` commands. Encourage explicit `--project-directory`, ordered `--file`, `--project-name`, `--profile`, and `--env-file` whenever the exact compose context is unknown or ambiguous; persist the canonical context tuple, env fingerprint, rendered-config artifact, runtime service snapshot, and storage-surface snapshot for later phases. Do not hand-roll Compose merge or interpolation rules, and do not dump raw config by default.</action>
  <verify>go test ./... -run 'TestInspect|TestCanonical'</verify>
  <done>Successful inspect calls persist canonical artifacts, while ambiguous or blocked contexts remain visible with explicit reasons instead of hidden failure modes.</done>
</task>

<task type="auto">
  <name>Task 2: Add summary-first inspect output and HOST-03 validation coverage</name>
  <files>internal/projects/inspect/output.go, internal/projects/inspect/service_test.go, test/integration/compose_inspect_test.go, test/e2e/remote_host_inspect_test.go, docs/testing.md</files>
  <action>Render human-readable inspect output so the first screen answers: what host is this, what project is this, does the tool trust the host and canonical render, which services are in scope, and what storage surfaces or issues matter. Add explicit `--show-config`, `--show-mounts`, and `--json` drill-downs, plus unit, Compose integration, and env-gated remote-host E2E tests for canonical, runtime-only, ambiguous, blocked, profile-dependent, and mixed-storage scenarios. Document the local and remote test commands in `docs/testing.md`, including how the env-gated host lane is enabled without breaking default autonomous execution.</action>
  <verify>go test ./...</verify>
  <done>Inspect output is concise by default, deep when requested, and backed by automated coverage for the risk cases called out in the research.</done>
</task>

<task type="auto">
  <name>Task 3: Update operator docs and planning artifacts after the Phase 1 foundation lands</name>
  <files>README.md, .gitignore, .planning/STATE.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/01-remote-host-registration-and-canonical-compose-discovery/01-VALIDATION.md</files>
  <action>Write a Phase 1 quickstart in the README covering host registration, discovery, inspect flows, local state locations, and artifact paths. Ignore generated SQLite databases and artifact snapshots in `.gitignore`. Update `STATE.md`, `ROADMAP.md`, and `REQUIREMENTS.md` so they record the now-fixed implementation decisions and mark HOST-01, HOST-02, and HOST-03 as implemented rather than merely planned. Replace the `01-VALIDATION.md` TBD rows and Wave 0 placeholders with the actual plan IDs, commands, and coverage lanes produced by execution so Phase 1 exits with a usable validation contract for later work.</action>
  <verify>go test ./... &amp;&amp; go build ./cmd/shum</verify>
  <done>The repo has operator-facing docs, ignored local artifacts, and planning documents that accurately describe the delivered Phase 1 foundation and its validation contract.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `go test ./...` passes
- [ ] `go build ./cmd/shum` succeeds
- [ ] `shum project inspect <ssh-alias> <project-ref>` shows trust, identity, canonical status, services, and storage surfaces or clearly explains why canonicalization is blocked
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- HOST-03 is implemented with canonical Compose inspection, persisted context artifacts, summary-first output, and updated docs and planning records for downstream phases
</success_criteria>

<output>
After completion, create `.planning/phases/01-remote-host-registration-and-canonical-compose-discovery/01-03-SUMMARY.md`
</output>
