---
phase: 01-remote-host-registration-and-canonical-compose-discovery
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - internal/cli/project.go
  - internal/cli/project_discover.go
  - internal/projects/status.go
  - internal/projects/repository.go
  - internal/projects/discovery/model.go
  - internal/projects/discovery/resolver.go
  - internal/projects/discovery/service.go
  - internal/projects/discovery/output.go
  - internal/projects/discovery/service_test.go
  - internal/projects/discovery/output_test.go
  - test/integration/compose_discovery_test.go
  - test/fixtures/compose/README.md
autonomous: true
requirements:
  - HOST-02
user_setup: []
must_haves:
  truths:
  - "`shum project discover <ssh-alias>` lists Docker Compose project candidates for a registered host using runtime-first discovery."
  - "Discovery never guesses a canonical Compose context from filenames alone; it marks runtime-only, ambiguous, or blocked states explicitly."
  - "Operator-selected path discovery only examines explicit directories and records enough project inventory for inspect and later phases to reuse."
  - "Discovered items include service counts and runtime context so inspect and later phases can show operator-relevant inventory immediately."
  artifacts:
    - path: "internal/cli/project_discover.go"
      provides: "CLI entrypoint for project discovery"
      min_lines: 60
      contains: "NewProjectDiscoverCommand"
    - path: "internal/projects/status.go"
      provides: "Shared project status model for canonical, runtime_only, ambiguous, and blocked states"
      min_lines: 30
      contains: "runtime_only"
    - path: "internal/projects/repository.go"
      provides: "Durable project and discovery snapshot persistence"
      min_lines: 60
      contains: "discovery"
    - path: "internal/projects/discovery/service.go"
      provides: "Runtime-first discovery service using remote docker compose and label inventory"
      min_lines: 100
      contains: "docker compose ls"
    - path: "internal/projects/discovery/resolver.go"
      provides: "Explicit path resolution for Compose defaults and ambiguous cases"
      min_lines: 60
      contains: "compose.yaml"
    - path: "test/integration/compose_discovery_test.go"
      provides: "Local Compose integration coverage for discovery behavior"
      min_lines: 60
      contains: "Test"
  key_links:
    - from: "internal/cli/project_discover.go"
      to: "internal/projects/discovery/service.go"
      via: "Project discovery command delegates to discovery service"
      pattern: "discovery\\.(NewService|Service)|Discover\\("
    - from: "internal/projects/discovery/service.go"
      to: "internal/remote/runner.go"
      via: "Discovery runs remote Docker and Compose inventory commands through the shared runner"
      pattern: "remote\\.(NewRunner|Runner)|docker compose ls|com\\.docker\\.compose\\.project"
    - from: "internal/projects/discovery/service.go"
      to: "internal/projects/repository.go"
      via: "Discovery persists candidate inventory and snapshot metadata"
      pattern: "repository|Save|Upsert"
---

<objective>
Implement runtime-first Docker Compose discovery for registered hosts without guessing canonical context.

Purpose: Later inspect and upgrade planning work only stays trustworthy if discovery surfaces candidates and uncertainty clearly instead of pretending it already knows the exact Compose execution context.
Output: Project discovery commands, shared project status and persistence models, path-resolution helpers, and automated HOST-02 coverage.
</objective>

<execution_context>
@/home/max/.codex/get-shit-done/workflows/execute-plan.md
@/home/max/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-remote-host-registration-and-canonical-compose-discovery/01-CONTEXT.md
@.planning/phases/01-remote-host-registration-and-canonical-compose-discovery/01-RESEARCH.md
@.planning/phases/01-remote-host-registration-and-canonical-compose-discovery/01-VALIDATION.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Build the shared project inventory model and runtime-first discovery service</name>
  <files>internal/projects/status.go, internal/projects/repository.go, internal/projects/discovery/model.go, internal/projects/discovery/service.go</files>
  <action>Create the shared project status model and repository layer that discovery and inspect will both use. Implement discovery so it starts from `docker compose ls --all --format json` on registered hosts and falls back to Compose labels from `docker container ls --all --filter label=com.docker.compose.project --format json` when runtime resources exist without a ready-made Compose listing. Persist discovery snapshots and candidate metadata, but keep runtime inventory distinct from canonical context so HOST-02 stops at trustworthy discovery rather than fake certainty.</action>
  <verify>go test ./... -run 'TestDiscovery|TestProjectStatus'</verify>
  <done>Registered hosts can produce persisted project candidates with explicit canonical, runtime-only, ambiguous, or blocked status metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Implement project discovery CLI output and explicit path resolution</name>
  <files>internal/cli/project.go, internal/cli/project_discover.go, internal/projects/discovery/resolver.go, internal/projects/discovery/output.go</files>
  <action>Add the `project` command namespace plus `project discover &lt;ssh-alias&gt;` with concise human-readable summaries and explicit `--json` output. Support opt-in `--path` discovery only for operator-specified directories; honor `.env` and `COMPOSE_FILE` only when they are part of that explicit directory context, otherwise limit probing to Compose default filenames and require operator-supplied `--file` ordering for multi-file cases. Avoid whole-host crawling, filename heuristics, or implicit profile guesses.</action>
  <verify>go test ./... -run 'TestProjectDiscover|TestPathResolution'</verify>
  <done>Discovery has a real CLI surface, explicit path probing, and summary output that tells the operator what is known, what is missing, and why.</done>
</task>

<task type="auto">
  <name>Task 3: Add Compose integration fixtures and HOST-02 coverage</name>
  <files>internal/projects/discovery/service_test.go, internal/projects/discovery/output_test.go, test/integration/compose_discovery_test.go, test/fixtures/compose/README.md</files>
  <action>Add deterministic unit tests for status classification and output rendering, plus local Compose integration tests that exercise runtime-first discovery, default filename resolution, missing file contexts, and ambiguous multi-file scenarios. Document fixture layout and Docker prerequisites in the Compose fixture README, and make tests skip with clear messages when Docker is unavailable so the plan stays executable on a clean repo while still leaving behind a serious integration lane.</action>
  <verify>go test ./...</verify>
  <done>HOST-02 is covered by unit and Compose integration tests, and the repo contains documented fixture conventions instead of ad hoc discovery test setup.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `go test ./...` passes
- [ ] `go build ./cmd/shum` succeeds
- [ ] `shum project discover <ssh-alias>` lists candidates with explicit statuses and never treats ambiguous contexts as canonical
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- HOST-02 is implemented with runtime-first discovery, explicit path-based fallback, and durable project inventory for later inspect work
</success_criteria>

<output>
After completion, create `.planning/phases/01-remote-host-registration-and-canonical-compose-discovery/01-02-SUMMARY.md`
</output>
