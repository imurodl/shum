---
phase: 01-remote-host-registration-and-canonical-compose-discovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - go.mod
  - go.sum
  - cmd/shum/main.go
  - internal/cli/root.go
  - internal/cli/host.go
  - internal/config/paths.go
  - internal/store/store.go
  - internal/store/migrations/001_initial.sql
  - internal/remote/runner.go
  - internal/ssh/config.go
  - internal/ssh/known_hosts.go
  - internal/ssh/probe.go
  - internal/hosts/service.go
  - internal/hosts/repository.go
  - internal/hosts/service_test.go
  - internal/ssh/config_test.go
  - internal/ssh/known_hosts_test.go
  - test/e2e/ssh_registration_test.go
  - test/fixtures/ssh/README.md
autonomous: true
requirements:
  - HOST-01
user_setup: []
must_haves:
  truths:
    - "The repo boots as a real Go CLI with a durable local store and a `shum` entrypoint instead of placeholder files."
    - "`shum host register <ssh-alias>` succeeds only when OpenSSH host trust, batch-safe auth, remote Linux, Docker, and Docker Compose checks all pass."
    - "Registered hosts can be listed and inspected later with alias, resolved SSH metadata, trust details, and last verification timestamps."
  artifacts:
    - path: "go.mod"
      provides: "Go module and dependency baseline for the Phase 1 CLI"
      contains: "module "
    - path: "cmd/shum/main.go"
      provides: "CLI entrypoint that boots the root command"
      min_lines: 10
      contains: "func main()"
    - path: "internal/cli/host.go"
      provides: "Host register, list, and inspect commands"
      min_lines: 60
      contains: "NewHostCommand"
    - path: "internal/hosts/service.go"
      provides: "Trusted host registration workflow and persistence orchestration"
      min_lines: 80
      contains: "Register("
    - path: "internal/ssh/probe.go"
      provides: "Strict OpenSSH probe and remote capability checks"
      min_lines: 60
      contains: "StrictHostKeyChecking=yes"
    - path: "internal/store/migrations/001_initial.sql"
      provides: "Initial schema for hosts, project records, and inspect artifacts"
      contains: "CREATE TABLE hosts"
    - path: "test/e2e/ssh_registration_test.go"
      provides: "Env-gated remote SSH registration E2E coverage"
      min_lines: 20
      contains: "SHUM_E2E_SSH_ALIAS"
  key_links:
    - from: "cmd/shum/main.go"
      to: "internal/cli/root.go"
      via: "CLI entrypoint constructs the root command"
      pattern: "cli\\.NewRootCommand"
    - from: "internal/cli/host.go"
      to: "internal/hosts/service.go"
      via: "Host command handlers delegate registration and inspection"
      pattern: "hosts\\.(NewService|Service)|Register\\(|List\\(|Inspect\\("
    - from: "internal/hosts/service.go"
      to: "internal/ssh/probe.go"
      via: "Host registration enforces strict SSH probing before persistence"
      pattern: "ssh.*StrictHostKeyChecking=yes|probe"
    - from: "internal/hosts/repository.go"
      to: "internal/store/store.go"
      via: "Host repository persists verified host records in SQLite"
      pattern: "store\\."
---

<objective>
Bootstrap the Go CLI foundation and implement trusted SSH host registration for Phase 1.

Purpose: Every later discovery, planning, and rollback guarantee depends on a durable, trustworthy host identity that fails closed on trust or auth problems.
Output: A real `shum` CLI, local SQLite-backed state, OpenSSH adapters, host register/list/inspect commands, and automated HOST-01 coverage.
</objective>

<execution_context>
@/home/max/.codex/get-shit-done/workflows/execute-plan.md
@/home/max/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-remote-host-registration-and-canonical-compose-discovery/01-CONTEXT.md
@.planning/phases/01-remote-host-registration-and-canonical-compose-discovery/01-RESEARCH.md
@.planning/phases/01-remote-host-registration-and-canonical-compose-discovery/01-VALIDATION.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Bootstrap the Go CLI runtime, config paths, and durable store</name>
  <files>go.mod, go.sum, cmd/shum/main.go, internal/cli/root.go, internal/config/paths.go, internal/store/store.go, internal/store/migrations/001_initial.sql</files>
  <action>Initialize the repo as a real Go module and create a thin `shum` CLI around a root command tree. Use an XDG-aware path helper for config, state, and artifact locations, and stand up a SQLite-backed store with a pure-Go driver so execution works in CI and on developer machines without CGO. Create the initial schema now for hosts, project inventory, discovery snapshots, and inspect artifacts so later plans extend stable tables instead of reworking storage ownership.</action>
  <verify>go test ./... &amp;&amp; go build ./cmd/shum</verify>
  <done>The repo has a buildable `shum` entrypoint, shared path helpers, and a durable local store foundation with real schema files.</done>
</task>

<task type="auto">
  <name>Task 2: Implement strict OpenSSH host registration and host inventory commands</name>
  <files>internal/cli/host.go, internal/remote/runner.go, internal/ssh/config.go, internal/ssh/known_hosts.go, internal/ssh/probe.go, internal/hosts/service.go, internal/hosts/repository.go</files>
  <action>Implement host registration around the system OpenSSH client instead of an in-process SSH stack. Resolve aliases with `ssh -G`, verify trust with `ssh-keygen -F` against the effective hostname and `[host]:port` form, and run a batch-safe probe equivalent to `ssh -o BatchMode=yes -o StrictHostKeyChecking=yes -T &lt;alias&gt; 'uname -s &amp;&amp; uname -m &amp;&amp; command -v docker &amp;&amp; docker compose version'`. Persist only verified hosts, capture resolved metadata plus Docker and Compose versions, and expose `host register`, `host list`, and `host inspect` with human-readable output by default and explicit `--json` support. Do not auto-trust `ssh-keyscan`, do not prompt, and do not hand-roll SSH config semantics.</action>
  <verify>go test ./... -run 'TestHost|TestSSH'</verify>
  <done>`shum host register` fails closed for unknown or changed host keys, interactive auth prompts, non-Linux targets, or missing Docker tooling, and successful registrations can be listed or inspected from persisted state.</done>
</task>

<task type="auto">
  <name>Task 3: Add HOST-01 unit coverage and env-gated remote SSH E2E scaffolding</name>
  <files>internal/hosts/service_test.go, internal/ssh/config_test.go, internal/ssh/known_hosts_test.go, test/e2e/ssh_registration_test.go, test/fixtures/ssh/README.md</files>
  <action>Add deterministic unit tests for `ssh -G` parsing, known-host matching, registration state transitions, and host output rendering. Add a real remote-host E2E test file that is gated behind explicit environment variables so the default plan remains autonomous while still proving a credible Linux SSH lane exists. Document the fixture alias, key, and known-host expectations in the SSH fixture README so Phase 1 execution leaves behind a repeatable trust test harness instead of a TODO.</action>
  <verify>go test ./...</verify>
  <done>HOST-01 has unit coverage for trust and parsing logic, plus a documented real-host E2E path that can be turned on without redesigning the test suite.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `go test ./...` passes
- [ ] `go build ./cmd/shum` succeeds
- [ ] `shum host register <ssh-alias>` persists verified hosts and rejects unknown or changed host keys plus interactive auth paths
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- HOST-01 is implemented with strict host-key verification, batch-safe auth enforcement, and persisted host metadata that later plans can trust
</success_criteria>

<output>
After completion, create `.planning/phases/01-remote-host-registration-and-canonical-compose-discovery/01-01-SUMMARY.md`
</output>
